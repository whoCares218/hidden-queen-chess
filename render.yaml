services:
  - type: web
    name: hidden-queen-chess
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --workers 1 --threads 16 --bind 0.0.0.0:$PORT app:app
